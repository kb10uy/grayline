# Every text file in this repository is stored and checked out with LF, on
# every platform. Declared here rather than left to each contributor's
# `core.autocrlf`, because a tool that writes a file in the platform's text
# mode would otherwise commit CRLF without anyone noticing.
* text=auto eol=lf

*.png binary
*.ico binary
*.jpg binary
*.webp binary
